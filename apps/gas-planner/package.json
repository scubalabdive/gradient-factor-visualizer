{
  "name": "gas-planner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Two Ceilings — bailout & minimum-gas planner. A decision-support tool that produces a gas ceiling (bar) and a time ceiling (OC bailout TTS) per GF set. Consumes the shared @gf/deco-engine and the pure @gf/gas-model.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@gf/deco-engine": "*",
    "@gf/gas-model": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
